# Dependências Python do conversor Convert2nc (Eta -> NetCDF)
# Instalação:  pip install -r requirements.txt

numpy
pandas
xarray
netCDF4          # escrita dos arquivos .nc (engine NETCDF4)

# --- GRIB2 sem wgrib2 (motor cfgrib, padrão para --grib) ---
# Lê GRIB2 direto em Python. Requer a lib de sistema ECCODES. O wheel 'eccodes'
# do PyPI já traz a lib na maioria dos casos; em cluster, o mais robusto é conda:
#   conda install -c conda-forge cfgrib eccodes
cfgrib
eccodes

# Opcional: só se usar --grib-engine wgrib2 (precisa de wgrib2 COM suporte NetCDF)
#   wgrib2 (binário do sistema, não via pip)
